name: CI/CD — PsySense Emotion AI

on:
  push:
    branches: [main, "copilot/**"]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.11"
  IMAGE_NAME: psysense-emotion-ai

jobs:
  # ── 1. Lint & tests ──────────────────────────────────────────────────────
  test:
    name: Lint & Test
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install \
            fastapi==0.111.0 \
            uvicorn[standard]==0.29.0 \
            pydantic==2.7.1 \
            pydantic-settings==2.2.1 \
            "torch==2.6.0" \
            "transformers==4.41.2" \
            safetensors==0.4.3 \
            scikit-learn==1.4.2 \
            numpy==1.26.4 \
            matplotlib==3.9.0 \
            prometheus-fastapi-instrumentator==7.0.0 \
            pytest==8.2.0 \
            pytest-asyncio==0.23.6 \
            httpx==0.27.0

      - name: Run tests
        run: pytest tests/ -v --tb=short

  # ── 2. Build Docker image ────────────────────────────────────────────────
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (smoke test — no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── 3. Deploy (main branch only) ─────────────────────────────────────────
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ── Uncomment and fill in secrets for your registry / cloud ──────────
      # - name: Log in to Amazon ECR
      #   uses: aws-actions/amazon-ecr-login@v2
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     AWS_REGION: us-east-1

      # - name: Push to ECR
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: ${{ secrets.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      # - name: Deploy to ECS / k8s
      #   run: |
      #     kubectl set image deployment/psysense-api \
      #       api=${{ secrets.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Deployment placeholder
        run: echo "Deploy step — configure your cloud provider credentials above."
